'use client';
import DesktopNavbar from '@/components/navbar/DesktopNavbar/DesktopNavbar';
import { Button } from 'antd';

const page = () => {
	return (
		<div>
			<DesktopNavbar>
				<h1>Home</h1>
			</DesktopNavbar>
		</div>
	);
};

export default page;
